# ores-otel-log-java-test

Exact-head **java** conformance harness for `ores-otel/ores.otel.log` and `ORESoftware/next-loggers.ts`.

Native verification command: `mvn --batch-mode --no-transfer-progress verify`.

Promotion requires both sources to pass at explicit 40-character commit SHAs.
